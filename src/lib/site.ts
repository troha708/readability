/**
 * Canonical production origin. Used for metadata, structured data, the
 * sitemap, and share links — anywhere a link must point at the real website
 * regardless of where the code is running (dev server, preview deploy, or
 * the mobile app).
 */
export const SITE_URL = "https://readability.bible";
